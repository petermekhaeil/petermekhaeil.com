---
import BaseLayout from '../layouts/base.astro';
import { getGitHubTilRepo } from '../_data/github_til_repo';

const til = await getGitHubTilRepo();

const pageDescription =
  'This is a collection of short notes of the things I have learned on a daily basis while working on different technologies. I share these notes as I learn in public.';
---

<BaseLayout
  title="Today I Learned"
  description={pageDescription}
  subTitle={pageDescription}>
  <div class="mx-auto mt-8 mb-16 prose-headings:mt-0">
    <p class="mb-4">
      This is a collection of short notes of the things I have learned on a
      daily basis while working on different technologies. I share these notes
      as I <a
        class="text-orange-500 dark:text-orange-400"
        href="https://www.learninpublic.org/"
        target="noopener">learn in public</a
      >.
    </p>
    <p>
      <i> <strong>{til.length}</strong> TILs and counting...</i>
    </p>

    <p class="mb-12">
      You can follow along by watching my <a
        class="text-orange-500 dark:text-orange-400"
        href="https://github.com/petermekhaeil/til"
        >GitHub repository<a></a>.
      </a>
    </p>

    <ul class="list-none space-y-2">
      {
        til.map((item) => (
          <li>
            <div class="flex">
              <div class="shrink-0 basis-28 text-zinc-400 dark:text-zinc-500">
                {item.date}
              </div>
              <h3 class="font-medium tracking-tight">
                <a
                  class="inline-fle text-black hover:bg-orange-500 dark:text-gray-100 dark:hover:text-black"
                  href={`/til/${item.path}/`}>
                  {item.title}
                </a>
              </h3>
            </div>
          </li>
        ))
      }
    </ul>
  </div>
</BaseLayout>
