---
layout: base.njk
eleventyNavigation:
  key: Blog
  order: 100
---

<div class="mt-4 lg:mt-10 mb-16 mx-auto max-w-2xl">

  <ul class="list-none space-y-8">
    {% for post in collections.posts | reverse %}
      <li>
        <a href="{{ post.url | url }}">
          <h3 class="w-full mb-2 font-medium text-indigo-700 text-lg md:text-xl dark:text-gray-100">
          {% if post.data.title %}
            {{ post.data.title }}
          {% else %}
            <code>{{ post.url }}</code>
          {% endif %}
          </h3>
        </a>
        <p class="text-gray-600 dark:text-gray-400">{{ post.data.description }}</p>
      </li>
    {% endfor %}
  </ul>

  <hr class="m-16" />

  <div class="lg:grid lg:grid-cols-12 lg:gap-8">
    <div class="lg:col-span-6 lg:flex lg:flex-col lg:justify-center space-y-6">
      <h2 class="font-medium text-black text-3xl dark:text-gray-100">Hi there 👋</h2>
      <p class="text-lg text-gray-600 dark:text-gray-400">I'm a software engineer with a passion for building performant web apps using the latest web technologies.</p>
      <p class="text-lg text-gray-600 dark:text-gray-400">This is my space where I'll share my learnings. I am a work in progress so feel free to send me feedback.<p>
    </div>
    <div class="sm:mx-auto lg:max-w-none lg:col-span-6">
      {% Image "src/assets/profile.png", "Handsome photo of Peter Mekhaeil on the Great Wall of China" %}
    </div>
  </div>
</div>